export default function Layout({ children }) {
    return (
        <div>
            <nav className="flex bg-white w-full">
                s
            </nav>
            <main>{children}</main>
        </div>
    );
}